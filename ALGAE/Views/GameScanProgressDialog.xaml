<Window x:Class="ALGAE.Views.GameScanProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Scanning for Games" 
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title and Status -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Scanning for Games" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding StatusMessage}" 
                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Progress Information -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16" 
                             Style="{StaticResource MaterialDesignOutlinedCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Grid.Row="0" Grid.Column="0" Kind="FolderSearch" 
                                         Width="20" Height="20" 
                                         VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentPath}" 
                           TextTrimming="CharacterEllipsis"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>

                <materialDesign:PackIcon Grid.Row="1" Grid.Column="0" Kind="FileDocument" 
                                         Width="20" Height="20" 
                                         VerticalAlignment="Center" Margin="0,8,12,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,8,0,0"
                           Style="{StaticResource MaterialDesignBody2TextBlock}">
                    <Run Text="Files: "/>
                    <Run Text="{Binding FilesScanned}"/>
                    <Run Text=" / "/>
                    <Run Text="{Binding TotalFiles}"/>
                </TextBlock>

                <materialDesign:PackIcon Grid.Row="2" Grid.Column="0" Kind="Controller" 
                                         Width="20" Height="20" 
                                         VerticalAlignment="Center" Margin="0,8,12,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,8,0,0"
                           Style="{StaticResource MaterialDesignBody2TextBlock}">
                    <Run Text="Games Found: "/>
                    <Run Text="{Binding GamesFound}" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                </TextBlock>

                <ProgressBar Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                             Margin="0,12,0,0"
                             Value="{Binding ProgressPercentage}"
                             Maximum="100"
                             Style="{StaticResource MaterialDesignLinearProgressBar}"/>
            </Grid>
        </materialDesign:Card>

        <!-- Directories List -->
        <GroupBox Grid.Row="2" Header="Scan Directories" 
                  Style="{StaticResource MaterialDesignGroupBox}"
                  Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ScanDirectories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <materialDesign:PackIcon Grid.Column="0" 
                                                         Kind="{Binding IconKind}" 
                                                         Width="16" Height="16" 
                                                         VerticalAlignment="Center" 
                                                         Margin="0,0,8,0"
                                                         Foreground="{Binding StatusColor}"/>
                                <TextBlock Grid.Column="1" 
                                           Text="{Binding Path}" 
                                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"
                    IsEnabled="{Binding CanCancel}"/>
            <Button Content="Close" 
                    Command="{Binding CloseCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    IsEnabled="{Binding IsCompleted}"/>
        </StackPanel>
    </Grid>
</Window>
