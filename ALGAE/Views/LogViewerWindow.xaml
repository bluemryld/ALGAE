<Window x:Class="ALGAE.Views.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ALGAE Log Viewer" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner"
        Icon="{StaticResource AppIcon}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <materialDesign:Card Grid.Row="0" Margin="8,8,8,4">
            <StackPanel Orientation="Horizontal" Margin="16,8">
                <materialDesign:PackIcon Kind="FileText" Width="24" Height="24" 
                                         VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Log Viewer" 
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                
                <ComboBox x:Name="LogLevelComboBox" 
                          SelectedItem="{Binding SelectedLogLevel}"
                          ItemsSource="{Binding LogLevels}"
                          Width="120" Margin="0,0,16,0"
                          ToolTip="Filter log level">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <TextBox x:Name="SearchTextBox" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="200" Margin="0,0,16,0"
                         materialDesign:HintAssist.Hint="Search logs..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                
                <Button Command="{Binding ClearLogsCommand}" 
                        Content="Clear" Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0"/>
                        
                <Button Command="{Binding RefreshCommand}" 
                        Content="Refresh" Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0"/>
                        
                <ToggleButton x:Name="AutoScrollToggle" IsChecked="{Binding AutoScroll}"
                              Content="Auto Scroll" 
                              Style="{StaticResource MaterialDesignSwitchToggleButton}"
                              Margin="0,0,8,0"/>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- Log Content -->
        <materialDesign:Card Grid.Row="1" Margin="8,4,8,4">
            <Grid>
                <ScrollViewer x:Name="LogScrollViewer" 
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding FilteredLogEntries}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                        BorderThickness="0,0,0,1" Padding="8,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" 
                                                   Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss.fff}'}"
                                                   FontFamily="Consolas" FontSize="11"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                        
                                        <TextBlock Grid.Column="1" Text="{Binding Level}"
                                                   FontFamily="Consolas" FontSize="11" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Critical">
                                                            <Setter Property="Foreground" Value="DarkRed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                            <Setter Property="Foreground" Value="Orange"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Information">
                                                            <Setter Property="Foreground" Value="Blue"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Debug">
                                                            <Setter Property="Foreground" Value="Gray"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Trace">
                                                            <Setter Property="Foreground" Value="LightGray"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        
                                        <TextBlock Grid.Column="2" Text="{Binding Category}"
                                                   FontFamily="Consolas" FontSize="11"
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                        
                                        <TextBlock Grid.Column="3" Text="{Binding Message}"
                                                   FontFamily="Consolas" FontSize="11"
                                                   TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </materialDesign:Card>
        
        <!-- Status Bar -->
        <materialDesign:Card Grid.Row="2" Margin="8,4,8,8">
            <StackPanel Orientation="Horizontal" Margin="16,8">
                <TextBlock Text="{Binding TotalEntries, StringFormat='Total: {0}'}" 
                           Margin="0,0,16,0"/>
                <TextBlock Text="{Binding FilteredCount, StringFormat='Filtered: {0}'}" 
                           Margin="0,0,16,0"/>
                <TextBlock Text="{Binding LastUpdate, StringFormat='Last Update: {0:HH:mm:ss}'}" 
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>